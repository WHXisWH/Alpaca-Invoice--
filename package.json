{
  "name": "alpaca-invoice-fhenix",
  "private": true,
  "version": "0.0.1",
  "packageManager": "pnpm@10.17.0",
  "scripts": {
    "dev:web": "pnpm --filter @alpaca/web dev",
    "dev:relayer": "pnpm --filter @alpaca/relayer dev",
    "build": "pnpm -r run build",
    "typecheck": "pnpm -r run typecheck",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "deploy:invoice-registry": "node scripts/deploy-invoice-registry.mjs",
    "smoke:create-invoice": "node scripts/smoke-create-invoice.mjs",
    "live:relayer-create": "node scripts/live-relayer-create.mjs",
    "drain:workers": "node scripts/drain-workers.mjs"
  },
  "devDependencies": {
    "solc": "^0.8.26",
    "typescript": "^5.6.3",
    "viem": "^2.21.54"
  }
}
