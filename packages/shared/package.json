{
  "name": "@alpaca/shared",
  "private": true,
  "version": "0.0.1",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "pnpm exec tsc -p tsconfig.json",
    "typecheck": "pnpm exec tsc --noEmit -p tsconfig.json",
    "lint": "echo \"shared lint not wired yet\"",
    "test": "echo \"shared tests not added yet\""
  },
  "dependencies": {
    "viem": "^2.21.54",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
